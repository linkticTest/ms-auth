{
    "name": "auth",
    "version": "0.1.0",
    "scripts": {
      "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
      "cdk": "cdk",
      "test": "jest"
    },
    "devDependencies": {
      "aws-cdk": "2.136.0"
    },
    "dependencies": {
      "aws-cdk-lib": "2.136.0",
      "constructs": "^10.0.0"
    }
  }